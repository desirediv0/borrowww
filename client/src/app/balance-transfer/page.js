import BalanceTransferClient from './BalanceTransferClient';

export const metadata = {
  title: 'Balance Transfer - Transfer Your Home Loan & Save | Borrowww',
  description:
    'Transfer your existing home loan to a new bank with lower interest rates and better terms. Zero hidden charges.',
};

export default function BalanceTransferPage() {
  return <BalanceTransferClient />;
}
